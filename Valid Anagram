class Solution {
public:
    bool isAnagram(string s, string t) {
        unordered_map<int,int> mp1,mp2;
        for(auto i:s) mp1[i]++;
        for(auto i:t) mp2[i]++;

        if(mp1 != mp2) return 0;
        return 1;
    }
};
